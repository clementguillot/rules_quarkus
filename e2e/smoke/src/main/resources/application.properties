# Smoke test configuration for rules_quarkus e2e tests
# The declared Bazel build property overrides this resource value and resolves
# its expression from a second, non-Quarkus declared key.
smoke.ext.prefix=CI
smoke.codegen.init-prefix=Configured\u0020
