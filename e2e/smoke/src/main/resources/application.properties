# Smoke test configuration for rules_quarkus e2e tests
# Overrides the smoke extension's build-time config default ("Smoke") to prove
# config flows through the extension recorder.
smoke.ext.prefix=CI
